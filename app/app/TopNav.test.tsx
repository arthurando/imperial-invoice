import { describe, it, expect } from 'vitest';
import { TopNav } from './TopNav';

describe('TopNav (contract)', () => {
  it('is importable', () => {
    expect(typeof TopNav).toBe('function');
  });
});
