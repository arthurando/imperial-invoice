import { describe, it, expect } from 'vitest';
import { DataSubNav } from './DataSubNav';

describe('DataSubNav (contract)', () => {
  it('is importable', () => {
    expect(typeof DataSubNav).toBe('function');
  });
});
