import { describe, it, expect } from 'vitest';
import { AddLineRow } from './AddLineRow';
describe('AddLineRow', () => {
  it('exports a component', () => {
    expect(typeof AddLineRow).toBe('function');
  });
});
